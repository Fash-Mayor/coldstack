import { LoginForm } from "./login-form";

export const metadata = {
  title: "Sign in | ColdStack",
  description: "Sign in or create your ColdStack account",
};

export default function LoginPage() {
  return <LoginForm />;
}
