import { OnboardingForm } from "./onboarding-form";

export const metadata = {
  title: "Onboarding | ColdStack",
  description: "Complete your ColdStack profile",
};

export default function OnboardingPage() {
  return <OnboardingForm />;
}
